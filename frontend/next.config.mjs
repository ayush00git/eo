/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['www.nith.ac.in',"res.cloudinary.com"], // Add other allowed domains here
      },
};

export default nextConfig;
